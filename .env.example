# VPS Backend API URL — no trailing slash
# Local dev: http://localhost:3000
# Production: set this on Cloudflare Pages Dashboard → Settings → Environment variables
VITE_API_BASE_URL=http://localhost:3000
